<h1>Posts</h1>

<% @post.reverse_each do |post| %>
<% user = User.find(post.user_id).name %>
  <h2><%= post.title %></h1>
  <% if user_signed_in?%>
    <p><%= user %></p>
  <% else %>
    <p>Anonim</p>
  <% end %>
  <div><%= link_to 'Show more', post %></div>
<% end %>